<p>Estimado docente:</p>

<p>{{ description }}</p>



<form action="{{ url }}" method="post">
  <input type="hidden" name="email" value="{{ email }}">
  <input type="hidden" name="request_id" value="{{ idRequest }}">
  
  <p>Nos encantaría contar con su aprobación. Por favor, seleccione una de las siguientes opciones:</p>
  <div>
    <input type="radio" id="option1" name="status" value="true">
    <label for="option1">Aceptar solicitud</label>
  </div>
  <div>
    <input type="radio" id="option2" name="status" value="false">
    <label for="option2">Rechazar solicitud</label>
  </div>
  
  <button type="submit" style="margin-top: 10px; background-color: blue; color: white; padding: 10px 20px; border: none; cursor: pointer;">Enviar</button>
</form>